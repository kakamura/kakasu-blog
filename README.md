# project

```
heroku git:remote -a kakasu-blog
git push heroku master

https://kakasu-blog.herokuapp.com/
```

```
npm install pug-cli; node node_modules/pug-cli/index.js views/ --out ./public
```

```
npm install firebase-tools
firebase login
firebase init // spacebar로 hosting 선택 후 enter, project 선택 (미리 만들어져 있어야 한다.)
firebase deploy
```